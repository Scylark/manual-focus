---
import Base from '../layouts/Base.astro';
import SEOHead from '../components/SEOHead.astro';
import FocusObject from '../components/FocusObject.astro';
import { url } from '../utils/paths';
---
<Base
  title="The Lens. A living library of AI marketing playbooks"
  description="The Lens. A curated, growing library of AI workflows, prompts and eval results for senior marketers. The methodology Manual Focus uses, in the open."
>
  <SEOHead
    slot="seo"
    type="service"
    title="The Lens. A living library of AI marketing playbooks"
    description="A curated, growing library of AI workflows, prompts and eval results for senior marketers. The methodology Manual Focus uses, search-first, refreshed as the models shift."
    breadcrumbs={[{ name: 'Home', url: '/' }, { name: 'The Lens', url: '/lens' }]}
  />

  <!-- HERO -->
  <section class="section lens-hero dwell-pin">
    <div class="dwell-pin-stage">
      <FocusObject size={420} tiltX={62} tiltY={-18} spin={140} scrollSpin={-80} style="top: 4%; right: 4%;" enterFrom="right" />
      <FocusObject size={300} tiltX={55} tiltY={20} spin={-110} scrollSpin={90} style="bottom: 6%; left: 3%;" enterFrom="left" />

      <div class="container lens-hero-inner">
        <div class="eyebrow reveal">000 · THE LENS</div>
        <h1 class="lens-headline reveal">
          Marketing,<br/>
          seen through a<br/>
          <span class="gradient-text">sharper lens</span>.
        </h1>
        <p class="lens-lead reveal">
          The AI marketing methodology Manual Focus uses, in the open.
          Prompts, pipelines, eval harnesses, failure modes, the full
          shape of how the work gets done. Drawn from the senior marketing
          practice of the founding team, refreshed as the models shift,
          searchable by what you're trying to ship.
        </p>
        <div class="lens-ctas reveal">
          <a href={url('/lens/start-here')} class="btn-cta">Start here</a>
          <a href={url('/lens/library')} class="btn-ghost">Browse the library</a>
        </div>
        <p class="lens-secondary-links reveal">
          New to AI? Read the <a href={url('/lens/primer')}>prompting 101</a> or the <a href={url('/lens/cowork')}>Cowork setup primer</a>.
        </p>
        <p class="lens-meta mono reveal">FREE · 46 playbooks · 25 skills · Endurance focus · Updated as the models shift</p>
      </div>
    </div>
  </section>

  <!-- THE GAP -->
  <section class="section section-alt dwell-pin">
    <div class="dwell-pin-stage">
      <FocusObject size={320} tiltX={60} tiltY={-22} spin={150} scrollSpin={-70} style="top: 8%; left: 4%;" enterFrom="left" />
      <div class="container">
        <div class="eyebrow">001 · THE GAP</div>
        <h2 class="big-statement reveal" data-scroll-scale style="transform-origin: left center;">
          Most senior marketers<br/>
          are using AI <span class="gradient-text">like a typewriter</span>.
        </h2>
        <div class="lens-grid">
          <p class="reveal">
            They draft a meta description in ChatGPT. They Midjourney a stock-photo
            replacement. They sit through webinars about agents and walk away
            with a list of tools they'll never actually wire up. The pipeline
            still looks like 2020 with a faster pen.
          </p>
          <p class="reveal">
            The operators a step ahead are using AI as a team. Briefs that
            self-iterate. Content systems that run themselves. Pipelines that
            improve every time a new model ships. The difference isn't talent
            or budget, it's having seen the workflow written down with all
            the failure modes labelled.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- WHAT IT IS -->
  <section class="section dwell-pin">
    <div class="dwell-pin-stage">
      <FocusObject size={360} tiltX={58} tiltY={18} spin={-130} scrollSpin={100} style="top: 6%; right: 4%;" enterFrom="right" />
      <div class="container">
        <div class="eyebrow">002 · WHAT IT IS</div>
        <h2 class="reveal">A living library, not a course you finish.</h2>
        <p class="lead reveal">
          The Lens is a curated reference for senior marketers. Each entry is
          a complete AI workflow shipped with the prompts, the evaluation
          harness, the failure modes and the actual eval results from the
          pipeline running in production. No ninety-minute video that loses
          relevance the moment a new model lands.
        </p>

        <div class="lens-pillars stagger">
          <article class="lens-pillar">
            <div class="pillar-num mono">/A</div>
            <h3>Workflows that ran in production</h3>
            <p>
              Every playbook in the library came out of a Manual Focus client
              engagement in the prior ninety days. Nothing theoretical, nothing
              "what if you tried", only patterns that held up when real money
              was on the line.
            </p>
          </article>
          <article class="lens-pillar">
            <div class="pillar-num mono">/B</div>
            <h3>Full eval harness, not just prompts</h3>
            <p>
              The prompts are the easy part. Each entry ships with the eval
              criteria, the test set, the failure cases and the score thresholds
              we use to decide a workflow is shippable. Swap the model, re-run
              the harness, ship.
            </p>
          </article>
          <article class="lens-pillar">
            <div class="pillar-num mono">/C</div>
            <h3>Refreshed as the models shift</h3>
            <p>
              When a frontier model lands we re-run the harness across every
              affected workflow. If it changes the result, the entry gets
              annotated and the prompts updated. The library stays current
              instead of going stale.
            </p>
          </article>
          <article class="lens-pillar">
            <div class="pillar-num mono">/D</div>
            <h3>Search-first, browse second</h3>
            <p>
              Filter by channel, by brand stage, by output type, by model.
              You're not reading the library cover to cover, you're pulling
              the playbook you need for the brief on your desk this week.
            </p>
          </article>
        </div>
      </div>
    </div>
  </section>

  <!-- PICK YOUR STARTING POINT -->
  <section class="section lens-first-time">
    <div class="container">
      <div class="eyebrow">003 · PICK YOUR STARTING POINT</div>
      <h2 class="reveal lens-first-headline">Match your situation, land on the playbook.</h2>
      <p class="lens-first-lead reveal">
        The Lens is 46 playbooks. Most readers don't need 46, they need
        the right one for the work in front of them. Match your
        situation below and you'll land on the right starter.
      </p>
      <div class="lens-first-grid stagger">
        <a href={url('/lens/brand/positioning-audit-pipeline')} class="first-card">
          <div class="first-card-tag mono">START WITH</div>
          <h3>I've inherited a brand and need an honest read</h3>
          <p>Positioning audit, voice extraction, message house. Five to ten working days end to end.</p>
          <span class="first-card-cue mono">POSITIONING AUDIT →</span>
        </a>
        <a href={url('/lens/content/eval-gated-drafting')} class="first-card">
          <div class="first-card-tag mono">START WITH</div>
          <h3>I need to ship more content without losing the brand</h3>
          <p>Eval-gated drafting feeding a social factory. Drafts pass voice, fact and originality gates before a human sees them.</p>
          <span class="first-card-cue mono">EVAL-GATED DRAFTING →</span>
        </a>
        <a href={url('/lens/ops/brief-to-ship-pipeline')} class="first-card">
          <div class="first-card-tag mono">START WITH</div>
          <h3>The team ships but the system around it is chaos</h3>
          <p>Brief-to-ship pipeline. Structured intake, weekly triage, weekly shipping cadence. Replaces ten tools.</p>
          <span class="first-card-cue mono">BRIEF-TO-SHIP →</span>
        </a>
        <a href={url('/lens/demand/channel-mix-simulator')} class="first-card">
          <div class="first-card-tag mono">START WITH</div>
          <h3>I need to defend a budget allocation to the CFO</h3>
          <p>Channel-mix simulator with reach-curve fitting. Walk into the meeting with three scenarios and the maths.</p>
          <span class="first-card-cue mono">CHANNEL-MIX SIMULATOR →</span>
        </a>
        <a href={url('/lens/productivity/daily-briefing-pipeline')} class="first-card">
          <div class="first-card-tag mono">START WITH</div>
          <h3>My day is reactive and I want it back</h3>
          <p>Daily briefing pipeline. Calendar plus inbox plus Slack plus CRM stitched into a five-minute morning brief.</p>
          <span class="first-card-cue mono">DAILY BRIEFING →</span>
        </a>
        <a href={url('/lens/start-here')} class="first-card first-card-fallback">
          <div class="first-card-tag mono">OR</div>
          <h3>None of those, show me the full guide</h3>
          <p>The "Start here" page has the by-situation lookup table, plus the three composition patterns and the install path.</p>
          <span class="first-card-cue mono">FULL GUIDE →</span>
        </a>
      </div>
    </div>
  </section>

  <!-- THE STACKS -->
  <section class="section section-alt dwell-pin" id="stacks">
    <div class="dwell-pin-stage">
      <FocusObject size={280} tiltX={50} tiltY={-15} spin={120} scrollSpin={-110} style="top: 8%; right: 5%;" enterFrom="right" />
      <div class="container">
        <div class="eyebrow">003 · THE STACKS</div>
        <h2 class="reveal">Five stacks. Each one growing.</h2>
        <p class="lead reveal">
          The library is organised by what you're trying to ship, not by
          tool. Pull what you need, ignore the rest.
        </p>
        <div class="lens-stacks stagger">
          <a href={url('/lens/brand')} class="lens-stack">
            <div class="stack-num mono">STACK · BRAND</div>
            <h3>Brand &amp; positioning</h3>
            <p>
              Positioning audits, naming sprints, brand-voice extraction
              (endurance extension), message houses, tagline systems, founder
              vs institutional voice.
            </p>
            <p class="stack-count mono">7 playbooks · growing →</p>
          </a>
          <a href={url('/lens/demand')} class="lens-stack">
            <div class="stack-num mono">STACK · DEMAND</div>
            <h3>Demand &amp; growth</h3>
            <p>
              Channel-mix, race-day demand, ambassador programmes, event
              sponsorship, retail partner programmes, subscription /
              membership, direct-to-coach, lifecycle journeys, CEP research.
            </p>
            <p class="stack-count mono">11 playbooks · growing →</p>
          </a>
          <a href={url('/lens/content')} class="lens-stack">
            <div class="stack-num mono">STACK · CONTENT</div>
            <h3>Content &amp; channels</h3>
            <p>
              SEO clusters, eval-gated drafting, earned-media pitches,
              social factories, training-content engines, race-result
              automation, segment b-roll, the AI-news pipeline.
            </p>
            <p class="stack-count mono">9 playbooks · growing →</p>
          </a>
          <a href={url('/lens/ops')} class="lens-stack">
            <div class="stack-num mono">STACK · OPS</div>
            <h3>Operating systems</h3>
            <p>
              Brief-to-ship pipelines, gear-launch sequences, end-of-season
              inventory, crash-replacement programmes, brand guardrails as
              code, customer content rights, evaluation frameworks, hiring.
            </p>
            <p class="stack-count mono">9 playbooks · growing →</p>
          </a>
          <a href={url('/lens/productivity')} class="lens-stack">
            <div class="stack-num mono">STACK · PRODUCTIVITY</div>
            <h3>Productivity &amp; back office</h3>
            <p>
              Daily briefings, email triage, meeting prep, call follow-up,
              pipeline rollups, OKR synthesis. The personal-assistant workflows
              that turn Cowork into a real second brain.
            </p>
            <p class="stack-count mono">10 playbooks · growing →</p>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- SKILLS -->
  <section class="section dwell-pin">
    <div class="dwell-pin-stage">
      <FocusObject size={300} tiltX={56} tiltY={-15} spin={140} scrollSpin={-80} style="top: 6%; right: 5%;" enterFrom="right" />
      <div class="container">
        <div class="eyebrow">004 · RUNNABLE COMPANIONS</div>
        <h2 class="reveal">Every playbook ships as a <span class="gradient-text">runnable skill</span>.</h2>
        <p class="lead reveal">
          The playbook explains the work. The skill is the work. Install the
          Lens skill pack into Claude Code, Cursor, Codex or any agent that
          honours the Agent Skills spec, and the workflow runs alongside the
          team.
        </p>
        <div class="lens-skills-cta reveal">
          <a href={url('/lens/skills')} class="btn-cta">Browse the skills</a>
          <a href="https://github.com/Scylark/manual-focus/tree/main/lens-skills" target="_blank" rel="noopener" class="btn-ghost">View on GitHub ↗</a>
        </div>
        <p class="lens-skills-note reveal">
          25 skills live · Plugin-installable · Welcome skill recommends a starter playbook on first ask (<a href={url('/lens/capabilities')}>capability reference</a>)
        </p>
      </div>
    </div>
  </section>

  <!-- INSIDE EACH ENTRY -->
  <section class="section section-alt dwell-pin">
    <div class="dwell-pin-stage">
      <FocusObject size={340} tiltX={62} tiltY={-12} spin={170} scrollSpin={60} style="top: 8%; left: 4%;" enterFrom="left" />
      <div class="container">
        <div class="eyebrow">005 · INSIDE AN ENTRY</div>
        <h2 class="reveal">What's actually in each playbook.</h2>
        <p class="lead reveal">
          A playbook isn't a prompt. It's the full shape of a workflow
          shipped to a paying client and held to a defined standard. Every
          entry includes the same six artefacts.
        </p>
        <ul class="lens-anatomy stagger">
          <li><span class="mono">A</span><span><strong>The brief.</strong> The problem the workflow solves, the output it produces, the kind of brand it suits.</span></li>
          <li><span class="mono">B</span><span><strong>The pipeline.</strong> The full step-by-step, including which model, which tool, which human checkpoint, in which order.</span></li>
          <li><span class="mono">C</span><span><strong>The prompts.</strong> Every prompt, in working order, with the variables labelled and the system prompts intact.</span></li>
          <li><span class="mono">D</span><span><strong>The eval harness.</strong> The test set, the criteria, the scoring rubric, the thresholds to ship.</span></li>
          <li><span class="mono">E</span><span><strong>The failure modes.</strong> Where the workflow breaks, what tells you it's breaking, what to swap in when it does.</span></li>
          <li><span class="mono">F</span><span><strong>The receipts.</strong> The actual outputs we shipped to actual clients, with the brand name redacted and the permissions on file.</span></li>
        </ul>
      </div>
    </div>
  </section>

  <!-- WHO IT'S FOR -->
  <section class="section section-alt dwell-pin">
    <div class="dwell-pin-stage">
      <FocusObject size={320} tiltX={55} tiltY={20} spin={-160} scrollSpin={80} style="top: 6%; right: 4%;" enterFrom="right" />
      <div class="container">
        <div class="eyebrow">006 · WHO IT'S FOR</div>
        <h2 class="reveal">Senior operators only.</h2>
        <div class="lens-fit-grid">
          <div class="lens-fit reveal">
            <h3>Built for</h3>
            <ul>
              <li>Heads of marketing and CMOs at $5M–$200M brands</li>
              <li>Fractional CMOs running two to four portfolio clients</li>
              <li>Senior in-house operators owning brand, demand or growth</li>
              <li>Marketing leaders at AI-native startups who need depth, not hot takes</li>
            </ul>
          </div>
          <div class="lens-fit reveal">
            <h3>Not built for</h3>
            <ul>
              <li>Generalists trying to break into marketing</li>
              <li>Agencies looking to re-sell the workflows as their own</li>
              <li>Anyone hoping for a "five prompts to grow your business" listicle</li>
              <li>People who want to skim, not ship</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- WHO CURATES IT -->
  <section class="section dwell-pin">
    <div class="dwell-pin-stage">
      <FocusObject size={400} tiltX={58} tiltY={-18} spin={160} scrollSpin={100} style="top: 6%; right: 4%;" enterFrom="right" />
      <div class="container">
        <div class="eyebrow">007 · WHO CURATES IT</div>
        <h2 class="big-statement reveal" data-scroll-scale style="transform-origin: left center;">
          Operators<br/>
          <span class="gradient-text">running the work</span>,<br/>
          not selling the dream.
        </h2>
        <p class="lead reveal">
          The Lens is curated by the Manual Focus partners and a rotating cast
          from the Foundry collective. Fifty-plus years of senior marketing
          experience between them, across endurance brands, B2B SaaS, fintech
          and consumer launches. The playbooks are the methodology that
          experience produced, the shapes, the prompts, the eval gates, the
          failure modes the partners have seen go wrong and learned to
          design around.
        </p>
        <p class="reveal">
          As Manual Focus engagements compound, real receipts replace
          illustrative examples in each entry and the eval results become
          the actual eval results from those runs. When a workflow gets
          retired we say so on the entry and explain why. When a model
          change breaks a workflow we annotate within seventy-two hours
          of the model release.
        </p>
      </div>
    </div>
  </section>

  <!-- ACCESS -->
  <section class="section section-alt dwell-pin">
    <div class="dwell-pin-stage">
      <FocusObject size={300} tiltX={50} tiltY={-18} spin={130} scrollSpin={-100} style="top: 6%; right: 4%;" enterFrom="right" />
      <div class="container">
        <div class="eyebrow">008 · THE TERMS</div>
        <h2 class="reveal">Free. Email required.</h2>
        <p class="lead reveal">
          Every playbook, every skill, the capability reference, the
          AI-news pipeline, all of it free. The only thing we ask
          for is your email, so we can tell you when new entries
          ship and when existing ones get updated.
        </p>
        <div class="lens-price reveal">
          <div class="lens-price-inner">
            <div class="price-head">
              <div class="price-tag mono"><span>FREE ACCESS</span><span class="price-flag">EMAIL FOR UPDATES</span></div>
              <div class="price-amount">£0<span class="price-per">/forever</span></div>
              <p class="price-shape mono">No paywall · No credit card · Unsubscribe anytime</p>
            </div>
            <ul class="price-list">
              <li>Full read access to every stack and every playbook</li>
              <li>Skill plugin installable in Claude Code (free, open source under MIT)</li>
              <li>Notified when new playbooks ship</li>
              <li>Notified when existing entries get refreshed against new frontier models</li>
              <li>AI-studio news pipeline summary in your inbox weekly (optional)</li>
              <li>No upsell. No drip funnel. Just the library and the updates.</li>
            </ul>
            <form
              class="lens-signup-form"
              data-newsletter-form
              action="https://manual-focus.us20.list-manage.com/subscribe/post?u=PLACEHOLDER&amp;id=PLACEHOLDER"
              method="POST"
              target="_blank"
              novalidate
            >
              <label for="lens-email-signup" class="sr-only">Email address</label>
              <input
                type="email"
                id="lens-email-signup"
                name="EMAIL"
                placeholder="your@email.com"
                required
                autocomplete="email"
              />
              <button type="submit" class="btn-cta">Get free access</button>
            </form>
            <p class="price-note">
              We send roughly one email a week. <a href={url('/privacy')}>What we do with your data</a>.
              Or skip the email and read everything directly at <a href={url('/lens/library')}>/lens/library</a>.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="section dwell-pin">
    <div class="dwell-pin-stage">
      <FocusObject size={260} tiltX={55} tiltY={-22} spin={140} scrollSpin={70} style="top: 8%; left: 4%;" enterFrom="left" />
      <div class="container">
        <div class="eyebrow">009 · BEFORE YOU GO IN</div>
        <h2 class="reveal">The questions you'd be right to ask.</h2>
        <div class="lens-faq stagger">
          <details class="lens-faq-item">
            <summary>It's really free?</summary>
            <p>
              Yes. Every playbook, every skill, the capability reference
              and the AI-news pipeline. No paywall. No credit card. We
              ask for your email so we can let you know when new entries
              ship and existing ones get refreshed, but the library itself
              is fully open at <a href={url('/lens/library')}>/lens/library</a>
              even without signing up.
            </p>
          </details>
          <details class="lens-faq-item">
            <summary>What's in it for Manual Focus, then?</summary>
            <p>
              The Lens is the brand. Senior marketers who use the playbooks
              sometimes find that work emerges that fits a Manual Focus
              engagement, whether Fractional CMO, Strategic Advisor, Build
              and Hand Over, or the AI product. A free, opinionated,
              regularly updated library is how we earn the right to that
              conversation. If you never need us, that's also fine.
            </p>
          </details>
          <details class="lens-faq-item">
            <summary>How is this different from a course?</summary>
            <p>
              A course is content you consume once. The Lens is a reference
              library you keep coming back to. Pull a playbook the day a
              brief lands, swap the model when a new one ships, hand a
              workflow to a junior with the eval harness intact. The shape
              is closer to a Bloomberg Terminal than a Udemy unit.
            </p>
          </details>
          <details class="lens-faq-item">
            <summary>How many playbooks are in there now?</summary>
            <p>
              46 live across the five stacks of Brand, Demand, Content,
              Ops and Productivity, plus 25 runnable skills installable
              as a Claude Code plugin (24 workflow skills plus the
              setup-the-lens welcome skill) and a capability reference
              page that grades what AI can actually do this quarter.
              New entries ship regularly as new patterns prove themselves
              in live work.
            </p>
          </details>
          <details class="lens-faq-item">
            <summary>Can I share with my team?</summary>
            <p>
              Yes. Pass the URLs around. Install the plugin on every
              team member's Claude Code. The library is meant to be
              used by working teams, not gate-kept.
            </p>
          </details>
          <details class="lens-faq-item">
            <summary>What if a model change breaks a workflow I'm running?</summary>
            <p>
              We annotate the affected entries within seventy-two hours of
              a frontier-model release. The annotation says what changed,
              which prompts were updated, what the new eval scores look
              like and whether the workflow still ships. If your version
              is running fine, you don't need to touch it.
            </p>
          </details>
          <details class="lens-faq-item">
            <summary>Is this a replacement for hiring Manual Focus?</summary>
            <p>
              For teams that can run the playbooks themselves, yes, this
              is the leverage without the cost of a Fractional CMO. For
              others it sits alongside one of the services as a way to
              keep the in-house team sharp between engagements.
            </p>
          </details>
        </div>
      </div>
    </div>
  </section>

  <!-- CLOSE -->
  <section class="section cta-close dwell-pin">
    <div class="dwell-pin-stage">
      <FocusObject size={520} tiltX={60} tiltY={-20} spin={160} scrollSpin={100} style="top: -10%; left: -10%;" enterFrom="left" />
      <FocusObject size={360} tiltX={50} tiltY={25} spin={-130} scrollSpin={-90} style="bottom: -10%; right: -8%;" enterFrom="right" />

      <div class="container cta-close-inner">
        <h2 class="cta-headline reveal">
          Read the work.<br/>
          <span class="gradient-text">Or pick one playbook and ship.</span>
        </h2>
        <p class="cta-sub reveal">
          The Lens compounds when used, not when read. Start with the
          guide if you're new, or jump straight to the library if you
          know what you're after.
        </p>
        <div class="cta-close-buttons reveal">
          <a href={url('/lens/start-here')} class="btn-cta">Start here</a>
          <a href={url('/lens/library')} class="btn-ghost">Browse the library</a>
        </div>
      </div>
    </div>
  </section>
</Base>

<style>
  /* ============================
     LENS HERO
     ============================ */
  /* Hero shares the same container width as the rest of the
     page (1280px from global .container) so the H1 and eyebrow
     align with the section headlines below it instead of sitting
     ~280px further in from the viewport edge. */
  .lens-headline {
    font-size: clamp(2.5rem, 7vw, 6rem);
    line-height: 1.02;
    letter-spacing: -0.02em;
    margin-top: var(--space-sm);
    color: var(--color-text);
  }
  .lens-lead {
    font-size: var(--font-size-lg);
    line-height: 1.5;
    color: var(--color-text-secondary);
    max-width: 60ch;
    margin-top: var(--space-md);
  }
  .lens-ctas {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
    margin-top: var(--space-md);
  }
  .lens-secondary-links {
    margin-top: var(--space-sm);
    font-size: var(--font-size-body);
    color: var(--color-text-secondary);
  }
  .lens-secondary-links a {
    color: var(--color-text);
    text-decoration: underline;
    text-decoration-color: var(--color-cta-start);
    text-underline-offset: 0.2em;
  }
  .lens-secondary-links a:hover {
    text-decoration-thickness: 2px;
  }
  .lens-meta {
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    color: var(--color-text-muted);
    margin-top: var(--space-md);
  }

  /* Two-column problem grid */
  .lens-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
    margin-top: var(--space-md);
    max-width: 90ch;
  }
  .lens-grid p {
    font-size: var(--font-size-body);
    line-height: 1.55;
    color: var(--color-text-secondary);
  }

  /* Pillars grid, 2x2 */
  .lens-pillars {
    margin-top: var(--space-lg);
    display: grid;
    grid-template-columns: 1fr;
    gap: 1px;
    background: var(--color-line);
    border: 1px solid var(--color-line);
    border-radius: 4px;
    overflow: hidden;
    position: relative;
    z-index: 2;
  }
  .lens-pillar {
    background: var(--color-bg);
    padding: var(--space-md);
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }
  .pillar-num {
    font-size: 0.7rem;
    color: var(--color-text-secondary);
    letter-spacing: 0.18em;
    margin-bottom: var(--space-xs);
  }
  .lens-pillar h3 {
    font-size: clamp(1.25rem, 1.8vw, 1.75rem);
    margin-bottom: 0.25rem;
  }
  .lens-pillar p {
    color: var(--color-text-secondary);
    font-size: var(--font-size-body);
    line-height: 1.5;
  }

  /* The Stacks */
  .lens-stacks {
    margin-top: var(--space-lg);
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
  }
  .lens-stack {
    padding: var(--space-md);
    border: 1px solid var(--color-line);
    border-radius: 4px;
    background: var(--color-bg);
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    position: relative;
    z-index: 2;
    text-decoration: none;
    transition: border-color 0.2s ease, background 0.2s ease, transform 0.2s ease;
  }
  .lens-stack:hover {
    border-color: var(--color-cta-start);
    background: var(--color-bg-alt);
    transform: translateY(-2px);
  }
  .lens-stack h3 {
    color: var(--color-text);
  }
  .stack-num {
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    color: var(--color-cta-start);
    margin-bottom: var(--space-xs);
  }
  .lens-stack h3 {
    font-size: clamp(1.25rem, 1.8vw, 1.6rem);
  }
  .lens-stack p {
    color: var(--color-text-secondary);
    font-size: var(--font-size-body);
    line-height: 1.5;
  }
  .stack-count {
    font-size: 0.7rem;
    color: var(--color-text-muted);
    letter-spacing: 0.12em;
    margin-top: auto;
    padding-top: var(--space-sm);
    border-top: 1px solid var(--color-line);
  }

  /* First-time-here grid */
  .lens-first-time {
    padding-block: var(--space-xl) var(--space-lg);
    border-bottom: 1px solid var(--color-line);
  }
  .lens-first-headline {
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 1.1;
    color: var(--color-text);
    margin-block: var(--space-sm);
  }
  .lens-first-lead {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    line-height: 1.55;
    max-width: 65ch;
    margin-bottom: var(--space-lg);
  }
  .lens-first-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
    margin-top: var(--space-lg);
  }
  @media (min-width: 769px) {
    .lens-first-grid { grid-template-columns: 1fr 1fr; }
  }
  @media (min-width: 1024px) {
    .lens-first-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  }
  .first-card {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    padding: var(--space-md);
    border: 1px solid var(--color-line);
    border-radius: 6px;
    background: var(--color-bg);
    text-decoration: none;
    transition: border-color 0.2s ease, transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
    cursor: pointer;
    position: relative;
    overflow: hidden;
  }
  .first-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--color-cta-gradient);
    opacity: 0;
    transition: opacity 0.2s ease;
  }
  .first-card:hover {
    border-color: var(--color-cta-start);
    background: var(--color-bg-alt);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
  }
  .first-card:hover::before { opacity: 1; }
  .first-card-tag {
    font-size: 0.65rem;
    letter-spacing: 0.18em;
    color: var(--color-text-muted);
  }
  .first-card h3 {
    font-size: clamp(1.1rem, 1.4vw, 1.35rem);
    color: var(--color-text);
    line-height: 1.25;
    margin: 0;
    transition: color 0.2s ease;
  }
  .first-card:hover h3 {
    color: var(--color-cta-start);
  }
  .first-card p {
    color: var(--color-text-secondary);
    line-height: 1.55;
    font-size: var(--font-size-body);
    flex: 1 1 auto;
    margin: 0;
  }
  .first-card-cue {
    font-size: 0.65rem;
    letter-spacing: 0.18em;
    color: var(--color-cta-start);
    margin-top: var(--space-sm);
    padding-top: var(--space-sm);
    border-top: 1px solid var(--color-line);
  }
  .first-card-fallback {
    background: var(--color-bg-alt);
  }

  /* Email signup form */
  .lens-signup-form {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    margin-bottom: var(--space-md);
  }
  @media (min-width: 600px) {
    .lens-signup-form {
      flex-direction: row;
      align-items: stretch;
    }
  }
  .lens-signup-form input[type="email"] {
    flex: 1 1 auto;
    padding: 0.85rem 1.2rem;
    font-size: 1rem;
    font-family: var(--font-family);
    background: var(--color-bg);
    color: var(--color-text);
    border: 1px solid var(--color-line);
    border-radius: 6px;
    transition: border-color 0.2s ease;
  }
  .lens-signup-form input[type="email"]:focus {
    outline: none;
    border-color: var(--color-cta-start);
  }
  .lens-signup-form input[type="email"]::placeholder {
    color: var(--color-text-muted);
  }
  .lens-signup-form .btn-cta {
    flex-shrink: 0;
    white-space: nowrap;
  }

  /* CTA close buttons */
  .cta-close-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
    justify-content: center;
    margin-top: var(--space-md);
  }

  /* Skills section CTA */
  .lens-skills-cta {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
    margin-top: var(--space-md);
  }
  .lens-skills-note {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    margin-top: var(--space-md);
    line-height: 1.55;
  }
  .lens-skills-note a {
    color: var(--color-cta-start);
    text-decoration: underline;
  }

  /* Anatomy of an entry */
  .lens-anatomy {
    list-style: none;
    padding: 0;
    margin: var(--space-lg) 0 0;
    display: grid;
    grid-template-columns: 1fr;
    gap: 1px;
    background: var(--color-line);
    border: 1px solid var(--color-line);
    border-radius: 4px;
    overflow: hidden;
    max-width: 900px;
  }
  .lens-anatomy li {
    background: var(--color-bg);
    padding: var(--space-md);
    display: grid;
    grid-template-columns: 2rem 1fr;
    gap: var(--space-sm);
    align-items: baseline;
    color: var(--color-text-secondary);
    line-height: 1.55;
  }
  .lens-anatomy .mono {
    color: var(--color-cta-start);
    font-size: 0.85rem;
    letter-spacing: 0.18em;
  }
  .lens-anatomy strong {
    color: var(--color-text);
    margin-right: 0.25rem;
  }

  /* Built for / not built for */
  .lens-fit-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
    margin-top: var(--space-lg);
  }
  .lens-fit {
    padding: var(--space-md);
    border: 1px solid var(--color-line);
    border-radius: 4px;
    background: var(--color-bg);
    position: relative;
    z-index: 2;
  }
  .lens-fit h3 {
    font-size: clamp(1.25rem, 1.8vw, 1.5rem);
    margin-bottom: var(--space-sm);
  }
  .lens-fit ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .lens-fit li {
    color: var(--color-text-secondary);
    line-height: 1.5;
    margin-bottom: var(--space-xs);
    padding-left: 1rem;
    position: relative;
  }
  .lens-fit li::before {
    content: '·';
    position: absolute;
    left: 0;
    color: var(--color-cta-start);
    font-weight: 700;
  }

  /* Pricing card */
  .lens-price {
    margin-top: var(--space-lg);
    max-width: 720px;
    position: relative;
    z-index: 2;
  }
  .lens-price-inner {
    border: 1px solid var(--color-line);
    border-radius: 6px;
    background: var(--color-bg);
    padding: var(--space-lg);
    position: relative;
    overflow: hidden;
  }
  .lens-price-inner::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--color-cta-gradient);
  }
  .price-head {
    margin-bottom: var(--space-md);
  }
  .price-tag {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: var(--space-sm);
    margin-bottom: var(--space-sm);
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    color: var(--color-text-secondary);
  }
  .price-flag {
    font-size: 0.625rem;
    letter-spacing: 0.14em;
    padding: 0.2rem 0.55rem;
    border: 1px solid var(--color-cta-start);
    color: var(--color-cta-start);
    border-radius: 999px;
    background: rgba(196, 90, 44, 0.06);
    white-space: nowrap;
    line-height: 1;
  }
  .price-amount {
    font-family: var(--font-display);
    font-size: clamp(3rem, 6vw, 5rem);
    line-height: 1;
    color: var(--color-text);
  }
  .price-per {
    font-family: var(--font-family);
    font-size: clamp(1rem, 1.4vw, 1.25rem);
    color: var(--color-text-secondary);
    font-weight: 400;
    margin-left: 0.5rem;
  }
  .price-shape {
    color: var(--color-text-secondary);
    font-size: 0.8rem;
    letter-spacing: 0.05em;
    margin-top: var(--space-xs);
  }
  .price-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-md);
    border-top: 1px solid var(--color-line);
    border-bottom: 1px solid var(--color-line);
    padding-block: var(--space-md);
  }
  .price-list li {
    color: var(--color-text-secondary);
    line-height: 1.5;
    margin-bottom: var(--space-xs);
    padding-left: 1.25rem;
    position: relative;
  }
  .price-list li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-cta-start);
    font-weight: 700;
  }
  .price-list li:last-child {
    margin-bottom: 0;
  }
  .price-foot {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }
  .price-note {
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  /* FAQ */
  .lens-faq {
    margin-top: var(--space-lg);
    display: flex;
    flex-direction: column;
    gap: 1px;
    background: var(--color-line);
    border: 1px solid var(--color-line);
    border-radius: 4px;
    overflow: hidden;
    position: relative;
    z-index: 2;
    max-width: 900px;
  }
  .lens-faq-item {
    background: var(--color-bg);
    padding: var(--space-md);
  }
  .lens-faq-item summary {
    cursor: pointer;
    list-style: none;
    font-size: clamp(1.05rem, 1.3vw, 1.25rem);
    font-weight: 600;
    color: var(--color-text);
    position: relative;
    padding-right: 2rem;
  }
  .lens-faq-item summary::-webkit-details-marker {
    display: none;
  }
  .lens-faq-item summary::after {
    content: '+';
    position: absolute;
    right: 0;
    top: 50%;
    transform: translateY(-50%);
    font-size: 1.5rem;
    color: var(--color-cta-start);
    transition: transform 0.2s ease;
    line-height: 1;
  }
  .lens-faq-item[open] summary::after {
    transform: translateY(-50%) rotate(45deg);
  }
  .lens-faq-item p {
    color: var(--color-text-secondary);
    line-height: 1.55;
    margin-top: var(--space-sm);
    max-width: 80ch;
  }

  /* ============================
     RESPONSIVE
     ============================ */
  @media (min-width: 769px) {
    .lens-grid { grid-template-columns: 1fr 1fr; gap: var(--space-lg); }
    .lens-pillars { grid-template-columns: 1fr 1fr; }
    .lens-stacks { grid-template-columns: 1fr 1fr; }
    .lens-fit-grid { grid-template-columns: 1fr 1fr; gap: var(--space-md); }
  }
  /* Five stack cards lay out as 3+2 at desktop instead of 2+2+1
     so the last card doesn't sit alone in its own row. */
  @media (min-width: 1024px) {
    .lens-stacks { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  }

  @media (max-width: 768px) {
    .lens-ctas { flex-direction: column; align-items: stretch; }
    .lens-ctas .btn-cta,
    .lens-ctas .btn-ghost { text-align: center; }
    .lens-price-inner { padding: var(--space-md); }
    .lens-anatomy li { grid-template-columns: 1.5rem 1fr; padding: var(--space-sm) var(--space-md); }
  }
</style>
